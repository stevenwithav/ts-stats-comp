// enum - could use object, but an enum signals the purpose to other engineers
// used if all values are known at compile time
export enum MatchResult {
	HomeWin = 'H',
	AwayWin = 'A',
	Draw = 'D',
}
